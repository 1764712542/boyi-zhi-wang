# 博弈之王 · 项目修改记录

> 本文档记录中国象棋项目各版本的修改内容、解决的问题和修复的bug，用于后续项目要求的参考。

---

## v5-overhaul · 大规模功能扩展

### 新增功能
- **故事模式角色解锁系统**：初始仅开放3角色（侯智博、周子翰、陆星辰），随章节推进逐步解锁，全通关后开放隐藏角色（B王、仙帝Alice、古月方圆）
- **棋子血量系统**：为每个棋子添加 hp/maxHp/atk/def 属性，吃子时结算伤害而非直接移除
- **虚拟形象系统**：重写 portrait.js，使用 SVG 内联头像替代外部文生图 API，解决加载错误
- **模式直接进入**：移除模式选择"下一步"按钮，点击模式卡片直接进入对应流程
- **图鉴全角色展示**：扩展 B王图鉴为全角色展示系统（18个角色）
- **阵营多色攻伐**：支持2-4色阵营在同一棋盘共存并互相攻伐
- **技能单体定向**：区分单体技能与全范围技能
- **大爱仙尊（古月方圆）重写**：技能改为"天命归一"+"扮猪吃虎"+"天道因果"，网络小说主角风格
- **B王阵营调整**：移除鸡哥，新增罗伦杰，B王新增2个主动技能（共5个）

### 解决的问题
- 虚拟形象第二页起加载错误（外部API限流）
- 模式选择点击卡片无响应
- PVP模式下被Ban角色仍显示
- 角色详情页被动面板无法显示

---

## v6-final-fixes · 最终修复

### 修复内容
- **虚拟形象重复字符渲染**：角色卡片同时渲染 SVG 内 text 和 .portrait-char div，导致双影重叠。移除3处重复 DOM 元素
- **B王技能补全**：为 B王 定义5个主动技能（装逼时刻、装逼领域、以退为进·本王版、先手夺人、偷梁换柱），图鉴展示全部5个技能，描述纯中文

---

## v7-rebalance · 兵种平衡性重构

### 新增功能
- **兵种类型分类**（PIECE_TYPE）：远程（炮）、进攻（车马）、防守（相士）、特殊（兵）、核心（帅）
- **兵种相克伤害系统**：
  - 炮(远程)打非远程：炮不掉血
  - 兵受非帅攻击：不掉血（后改为50%伤害）
  - 非炮打相/士：攻击方获虚弱buff
  - 兵打帅：+50%伤害
- **血量数值提升**：HP 提升3-5倍（帅300, 车180, 兵200等）
- **HUD状态栏**：显示buff列表、各方兵力统计、兵种相克提示
- **3主动+2被动技能系统**：每个角色3个主动技能（选1）+ 2个被动技能（选1），技能机制参考Dota2
- **棋子操作菜单**：选中棋子后显示「进攻」/「查看详情」
- **多人模式封闭**：4v4模式显示"即将开放"
- **阵营选人去重**：B王阵营改为含刘雪沛，跨阵营去重
- **故事模式解锁自由对局**：未完成故事模式时PVE锁定
- **新手教程系统**：5步教程引导

---

## v8-critical-fixes · 恶性Bug修复

### 修复的恶性Bug
- **炮伤害反向**：calcDamage 返回值的 attackerDmg/defenderDmg 应用反了，导致炮打车时炮自己掉血。修复变量互换
- **点击棋子无响应**：操作菜单拦截了 selectPiece，导致无法选棋。移除拦截，点击直接进入移动模式
- **棋盘过小**：CANVAS_H 从620增大到660，红方最后一排血条完整可见

### 新增功能
- **血量提升100倍**：HP 从3位数提升到5位数（帅30000等，后v9回调）
- **角色属性附着**：每个棋子继承所选角色属性（charAtk/charDef/charInt），calcDamage 使用 atk + charAtk/10
- **兵削弱**：受非帅攻击从完全免疫改为受50%伤害
- **车马加强**：攻击时无视30%防御（破甲效果）
- **PVP双方信息显示**：HUD 显示红黑双方 buff 和技能冷却状态

---

## v9-detail-fixes · 细节修复与文档化

### 修复内容
- **血量回调**：从5位数（30000等）回调到3位数（300等），保持合理游戏节奏
- **被动技能修复**：修复被动技能未生效的问题，确保各触发类型正确执行
- **HUD同步角色属性**：HUD 显示角色属性加成（charAtk/charDef/charInt）
- **选将技能选择面板**：点击角色卡片弹出技能选择面板（3主动选1 + 2被动选1）
- **棋子合并影响血量**：合并时血量叠加
- **新手教程更新**：涵盖解锁方式、兵种相克、技能选择、角色属性、buff系统

### 新增文档
- **项目修改记录.md**：记录 v5-v9 各版本修改内容，避免重复提醒

---

## v10-skill-redesign · 技能选择生效链路

### 修复内容
- **技能选择生效修复**：新增 `getActiveSkillForCurrentPlayer` 函数，`usePlayerSkill` 读取选将面板选中的技能而非默认 `char.skill`
- **替代主动技能实现**：补全 17 角色的替代主动技能（actives[1]/actives[2]）的 switch case
- **PVP 双方技能存储**：`confirmCharacterSelect` 保存 PVP 双方选中的主动/被动技能到 `state.pvpRedActiveSkill` / `state.pvpBlackActiveSkill`
- **新手教程 8 步**：扩展教程为 8 步，涵盖模式与解锁、HP 体系、技能选择等内容

---

## v11-skill-overhaul · 技能重设计与图鉴修复

### 修复的 Bug
1. **技能选择进入游戏后不生效**（用户反馈"选择了技能进入游戏后没有改变"）
   - 根因：`state.playerActiveSkill` 未在 state 初始化中显式声明
   - 根因：三英模式 `state.playerActiveSkill` 仅保存最后一位武将的技能，轮换时未切换
   - 根因：PVP 模式 HUD 对方技能显示仍用 `p2Char.skill.name`（默认技能）
   - 修复：state 初始化添加 `playerActiveSkill:null, playerPassiveSkill:null, threeHeroSkills:[]`
   - 修复：三英模式为每位武将独立存储技能，`switchThreeHero` 切换时同步技能
   - 修复：`startNewGame` 三英模式初始化时恢复第一位武将技能
   - 修复：PVP HUD 对方技能显示读取 `pvpBlackActiveSkill` / `pvpRedActiveSkill`

2. **角色图鉴不完整**
   - 根因：`renderCodexDetail` 中 `skillList` 仅取 `[ch.skill]`（默认第 1 个主动）
   - 修复：改为展示 `ch.actives`（3 个主动技能），B 王展示 `ch.skills`（5 个主动）
   - 修复：新增技能范围标签（全范围/单体）

3. **新手教程总步数硬编码**
   - 根因：index.html 中教程总步数硬编码为 `/5`
   - 修复：改为动态 `<span id="tutorial-step-total">`，`renderTutorialStep` 动态设置

### 新增文档
- **v11 技能重设计规格文档**（`.trae/specs/v11-skill-overhaul/`）：
  - `spec.md`：18 角色技能重设计总表（3 主动 + 2 被动，含 CD/类型/效果）
  - `tasks.md`：4 阶段任务分解
  - `checklist.md`：验收检查清单

### 技能重设计原则
- 信息/辅助型：CD 3，无直接伤害
- 战术型：CD 3-4，伤害 30-80
- 强攻型：CD 4-5，伤害 80-150
- 治疗/护盾：30-50% 最大 HP
- Buff/Debuff 持续：1-3 回合
- 角色智力（int）影响技能伤害：`最终伤害 = 基础伤害 × (1 + int/200)`

---

## v12-hud-sync · 状态栏同步与血量调整

### 修复内容
1. **状态栏不随数值提升而更新**（用户反馈"状态栏不会随着数值的提升而提升"）
   - 根因：HUD 只显示棋子基础 `atk`/`def`，未显示角色加成与 buff 影响后的最终值
   - 修复：新增 `getPieceEffectiveStats(piece)` 工具函数（engine.js），计算完整属性
   - 修复：HUD 新增"选中"区域，显示选中棋子的完整属性（HP/攻/防/buff 列表）
   - 修复：攻防显示为 `基础(+角色加成) → 最终值`，直观展示 buff 影响

2. **buff 未显示具体数值影响**
   - 根因：buff 列表只显示 buff 名字，不显示对攻防血的具体加成
   - 修复：新增 `BUFF_DESC_MAP` 和 `getBuffDesc(b)` 工具（game.js）
   - 修复：buff 列表显示 `名称(效果描述, 持续回合)`
   - 修复：棋子详情弹窗显示完整 buff 列表（名称/描述/剩余回合）

3. **棋子详情弹窗属性不完整**
   - 根因：`showPieceDetail` 只显示基础攻防，未显示角色加成和 buff 影响
   - 修复：显示 `基础(+加成) → 最终值`，新增"角色加成"行和 buff 详情列表

4. **血量过高导致游戏过长**（用户反馈"血量还是要降低一点点"）
   - 修复：血量再次降低约 40%
   - 帅：300→180 / 车：180→110 / 马：150→90 / 炮：120→75
   - 仕/相：100→60 / 兵：200→120
   - 保持 atk/def 不变，角色属性加成不受影响

### 新增工具函数
- `getPieceEffectiveStats(piece)`：计算棋子完整属性（基础+角色加成+buff影响）
- `getBuffDesc(b)`：返回 buff 的中文名称与效果描述
- `BUFF_DESC_MAP`：buff 类型 → 名称/描述映射表

### UI 改动
- HUD 新增"选中"区域（`#hud-selected-piece`）
- 棋子详情弹窗新增 buff 列表（`.pd-buffs`）
- 属性区域新增"智"显示（角色智力加成）

---

## v13-buff-system · 技能数值加成统一为 buff 系统

### 修复的 Bug
1. **选中棋子后 HUD 不显示**（用户反馈"选中没有用啊"）
   - 根因：`state.selected` 使用 `{row, col}` 字段，但 HUD 代码读取 `state.selected.r` / `state.selected.c`
   - 修复：兼容两种字段，优先读取 `{row, col}`

2. **Debug 扫描看不出 +50% 伤害**（用户反馈"感觉没有加 50% 的伤害一样，看不出来"）
   - 根因：技能数值加成写在 `state.executeMark` / `state.teamAtkBuff` 等标记位，但 `calcDamage()` 只读取棋子的 `buffs` 数组，不读取 state 标记——这些标记"写了但没用"
   - 修复：把所有技能数值加成转化为统一 buff 挂到棋子上，`calcDamage()` 统一读取

3. **技能数值加成未统一为 buff 系统**（用户反馈"每个加数值的技能都变成 buff，添加到棋子"）
   - 根因：buff 系统只有 `weakness`/`ironwall`/`shield` 等少数几种，技能加成没有进入 buff 系统
   - 修复：扩展 buff 类型，新增 `attackBoost`/`defenseBoost`/`executeMark`/`reflect`/`immune` 等

### 核心重构

#### 1. `calcDamage()` 统一读取 buff 系统
- 攻击方 buff：`weakness`（攻-N%）、`attackBoost`（攻+N）、`executeMark`（伤害+N%）
- 防守方 buff：`ironwall`（防×2）、`defenseBoost`（防+N）、`shield`（吸收N伤害）、`immune`（免疫所有伤害）
- 返回新增字段：`defenderImmune`、`reflectDmg`、`executeMarkBuff`
- `doMove()` 应用：免疫时双方不掉血、反伤 buff 反弹伤害、executeMark 攻击后消耗

#### 2. 新增 buff 工具函数（engine.js）
- `addBuff(piece, type, value, duration)`：给棋子加 buff（同类合并取较高 value）
- `addTeamBuff(board, player, type, value, duration)`：给一方全体棋子加 buff
- `consumeBuff(piece, type)`：消耗一次性 buff（如 executeMark）

#### 3. 技能重构为挂 buff
| 技能 | 原实现（state 标记） | 新实现（buff） |
|------|---------------------|----------------|
| 三金·狂战之怒 | state.ironwallTurns | ironwall + attackBoost buff 到选中棋子，2回合 |
| 三金·嗜血斩杀 | state.executeMark | executeMark buff 到价值最高己方攻击棋子 |
| 三金·兄弟连斩 | state.teamAtkBuff | attackBoost buff 到己方全体，2回合 |
| 陆星辰·Debug扫描 | state.executeMark | executeMark buff 到价值最高己方攻击棋子 |
| 胡浩·正道护体 | state.shieldMode | shield + defenseBoost buff 到选中棋子，3回合 |
| 胡浩·万法归一 | state.teamAtkBuff | attackBoost buff 到己方全体，2回合 |
| ikun·全给你 | state.reflectTurns | reflect buff 到己方全体，3回合 |
| 周子翰·优雅闪烁 | state.teleportBuff | attackBoost buff 到瞬移棋子，2回合 |
| 唐昊博涵·翻书作弊 | state.teamShield | shield buff 到己方全体，2回合 |
| 刘佳伟·后发制人 | state.reflectTurns | reflect buff 到己方全体，3回合 |
| 袁清山·龙跃九天 | state.teamAtkBuff | attackBoost buff 到己方全体，2回合 |
| 罗伦杰·无尽连斩 | state.teamAtkBuff | attackBoost buff 到己方全体，2回合 |
| 大爱仙尊·天命归一 | state.destinyTurns | attackBoost buff 到己方全体，3回合 |

#### 4. `getPieceEffectiveStats()` 新增 buff 类型显示
- executeMark：必中标记（伤害+50%）
- reflect：反伤（反弹50%伤害）
- immune：无敌（免疫所有伤害）

#### 5. `BUFF_DESC_MAP` 扩展
新增 executeMark/reflect/immune 三种 buff 的中文名称与描述

### 整体逻辑盘点
1. **技能释放** → `usePlayerSkill()` 中 switch case 处理，调用 `addBuff()` / `addTeamBuff()` 挂 buff
2. **选中棋子模式** → `selectPiece()` 中处理 ironwall/shieldMode，选中后挂 buff
3. **走棋攻击** → `doMove()` 调用 `calcDamage()`，统一读取 buff 系统计算伤害
4. **buff 消耗** → `doMove()` 中攻击命中后调用 `consumeBuff()` 消耗 executeMark
5. **buff 持续** → `tickBuffs()` 回合结束时递减 duration，归零移除
6. **HUD 显示** → `renderHUD()` 调用 `getPieceEffectiveStats()` 显示完整属性
7. **棋子详情** → `showPieceDetail()` 显示基础+加成+最终值+buff列表

---

## v14-inspect-and-forces · 对方棋子查看与兵力同步

### 修复的 Bug
1. **点击对方棋子无反应**（用户反馈"选中点击对方的棋子也要看到效果，不然没法判断 debuff 是否生效"）
   - 根因：`selectPiece()` 中点击非己方棋子时直接 return 清空 selected，不进入查看模式
   - 修复：新增 `state.inspect` 只读查看模式，点击对方棋子时设置 inspect，HUD 显示该棋子属性与 buff
   - HUD 头部新增"查看"标签（紫色），区分己方选中与对方查看

2. **兵力面板不反映 buff 和角色加成**（用户反馈"状态面板的兵力根本没有变化"）
   - 根因：兵力统计只累加 `p.atk` / `p.def` 基础值，未调用 `getPieceEffectiveStats` 计算有效属性
   - 修复：改用 `getPieceEffectiveStats(p)` 获取 `effAtk` / `effDef`，包含角色加成与 buff 影响
   - 新增 buff 计数显示（`buff×N`），直观展示双方当前 buff 数量

### 改动详情
- `selectPiece()`：点击对方棋子设置 `state.inspect={row,col}`，不进入选中模式
- `renderHUD()`：`target = state.selected || state.inspect`，两者都显示属性
- 兵力统计：`stats.totalAtk += eff.effAtk`（含 buff 加成）
- 新增 `.hud-sel-inspect` 样式（紫色"查看"标签）
- 新增 `.hud-buff-count` 样式（紫色 buff 计数）

### 验证场景
1. 释放 Debug 扫描 → 点击被标记的己方棋子 → HUD 显示"必中标记(伤害+50%, 1回)"
2. 释放兄弟连斩 → 兵力面板红方总攻立即提升（+24/棋子）
3. 点击对方被沉默的棋子 → HUD 显示"沉默(无法使用技能, N回)"
4. 释放狂战之怒选中己方车 → 兵力面板红方总防翻倍（铁壁 buff）




---

## 项目约定

### 角色设计
- 每个角色3个主动技能 + 2个被动技能
- B王有5个主动技能（覆盖3个难度）
- 技能机制参考Dota2（沉默/禁锢/闪烁/幻象等）
- 角色属性影响棋子战斗（charAtk/charDef/charInt）

### 兵种相克规则
- 炮(远程)打非远程：炮不掉血
- 兵受非帅攻击：受50%伤害
- 非炮打相/士：攻击方获虚弱buff（下回合攻击-30%）
- 兵打帅：+50%伤害
- 车马攻击：无视30%防御（破甲）

### 游戏模式
- PVE：未完成故事模式时锁定
- PVP：始终开放
- 故事模式：角色逐步解锁
- 阵营模式：B王阵营自动作为对方加入
- 4v4：暂时封闭

### 数据持久化
- 故事进度：localStorage.bky_story_progress
- 教程完成：localStorage.bky_tutorial_done
- 存档：localStorage.bky_save

---

## v15-bug-fixes · P0 致命 Bug 修复

### 三英模式字符串不匹配（致命）
- 问题：`skills.js` 中 `getActivePassives`、`getBkingPassives`、`triggerPeriodicPassives` 判断 `state.gameMode==='threeHeroes'`，实际取值为 `'three'`，导致三英模式被动全开失效。
- 修复：将上述三处字符串统一改为 `'three'`。
- 文件：`js/skills.js`

### 选将被动选择游戏开始后失效（致命）
- 问题：`resetPassives` 清空 `skillState.selected`，导致选将时选择的主动/被动索引在每局开始时丢失。
- 修复：`resetPassives` 不再清空 `skillState.selected`，仅重置 `passiveState`（运行时计数器）。
- 文件：`js/skills.js`

### 罗伦杰连击被动永不执行（致命）
- 问题：罗伦杰被动 id `p_chain` 与侯智博的 `p_chain` 重复，`switch` 永远匹配第一个 case，罗伦杰被动不触发。
- 修复：罗伦杰被动 id 改为 `p_chainatk`，同步更新 `data.js` 与 `skills.js`。
- 文件：`js/data.js`、`js/skills.js`

### state.playerExtraMove 只写不读（致命）
- 问题：侯智博、ikun 的被动写入 `state.playerExtraMove`，但流程控制读取 `state.extraMove`，标记名不一致导致连走两步失效。
- 修复：将被动写入改为 `state.extraMove`，与 doMove 流程读取一致。
- 文件：`js/skills.js`

---

## v16-bug-fixes · buff 系统与棋子生命周期 Bug 修复

### cloneBoard 浅克隆 buffs 导致仙帝回溯快照失效（致命）
- 问题：`cloneBoard` 使用 `{...c}` 浅克隆，buffs 数组共享引用。doMove 中 `calcDamage` 消耗 shield/executeMark 等修改 buff 时，`state.boardSnapshots` 中的快照 buff 状态被同步污染，仙帝回溯还原后 buff 错误。
- 修复：`cloneBoard` 深克隆 buffs 数组（`c.buffs.map(b=>({...b}))`）。
- 文件：`js/engine.js`

### calcDamage immune 时仍消耗 executeMark（严重）
- 问题：防守方持有 `immune` buff 时，`calcDamage` 在 immune 分支返回 `executeMarkBuff`，doMove 据此消耗攻击方的 `executeMark`。但 immune 表示攻击未命中，executeMark 不应被消耗。
- 修复：immune 分支返回 `executeMarkBuff:null`，保留攻击方的必中标记。
- 文件：`js/engine.js`

### addBuff 续期重置 _fresh 导致 buff 多活 1 回合（严重）
- 问题：`addBuff` 给已存在的 buff 续期时执行 `existing._fresh = true`，导致 `tickBuffs` 再次跳过本回合递减，续期 buff 实际多活 1 回合。
- 修复：续期时不再重置 `_fresh`，保留原值（已递减过的保持 false，新增的仍为 true）。
- 文件：`js/engine.js`

### passivesOnCapture 中 'threeHeroes' 字符串残留（严重）
- 问题：v15 修复时遗漏 `passivesOnCapture` 中三英反吃的判断 `state.gameMode==='threeHeroes'`，导致三英模式 B王反吃被动失效。
- 修复：统一改为 `'three'`。
- 文件：`js/skills.js`

### reviveLastPiece / summonPawn 不注入角色属性（严重）
- 问题：复活/召唤的棋子缺少 `charAtk/charDef/charInt/ptype` 字段，导致：
  - 复活后失去角色属性加成（攻防降低）
  - `getPieceEffectiveStats` 计算 charAtkBonus/charDefBonus 为 0
  - `calcDamage` 中 `attacker.ptype` 为 undefined，兵种相克规则失效
- 修复：`reviveLastPiece` 和 `summonPawn` 调用 `getCharBonus(charId)` 注入角色属性，并补全 `ptype` 字段。
- 文件：`js/skills.js`

### canUseSkill / updateSkillDisplay 硬编码 CD=3（严重）
- 问题：`canUseSkill` 与 `updateSkillDisplay` 一律按 3 回合 CD 判断就绪，但角色技能实际 CD 为 3/4/5。导致 CD 4 的「狂战之怒」、CD 5 的「仙帝·天罚」「完美伪装」等技能提前 1~2 回合可用，破坏平衡。
- 修复：
  - `canUseSkill`：读取 `getActiveSkillForCurrentPlayer().cd` 作为基础 CD，叠加 `bkingCdIncrease`（仙帝威压）与 `skillCdReduce`（掀桌/天道因果）修正。
  - `updateSkillDisplay`：玩家侧 CD 显示与 `canUseSkill` 同步；PVP 对方圆点数改为对方技能实际 cd。
- 文件：`game.js`

### 验证
- `node -c` 校验全部 JS 文件通过（ALL_OK）
- PVP 双方 buff 显示同步（renderHUD 遍历棋盘分别收集红黑 buff）
- 棋子详情弹窗正确显示有效攻防与 buff 列表（showPieceDetail 调用 getPieceEffectiveStats）

---

## v16-bug-fixes-ext · 分模块深度验证与修复

### 模块2·状态面板：renderHUD 技能 CD 硬编码（严重）
- 问题：`renderHUD` 中双方技能冷却显示硬编码 `3 - redCD` / `3 - blackCD`，与 v16 修复的 `canUseSkill`/`updateSkillDisplay` 不一致，导致 HUD 状态栏中 CD 4/5 的技能显示错误。
- 修复：按选中技能实际 cd 计算，叠加 `bkingCdIncrease`/`skillCdReduce` 修正，与 `canUseSkill` 完全同步。
- 文件：`game.js`

### 模块3·技能系统：5 个一次性标记只写不读（致命）
- 问题：以下技能标记在 `usePlayerSkill` 中设置后，从未被读取或清理，导致技能完全失效且标记永久残留：
  - `oppMissNext`（鸡哥·虚晃一枪）：对方下回合攻击打偏 — 未在 doMove 检查
  - `barrageActive`（三金·兄弟连斩）：吃子后可再走 — 未在 doMove 检查
  - `oppPassiveDisabled`（仙帝审判/逻辑爆破）：对方被动失效 — 未在 triggerPassive 检查
  - `aoeLockdownTurns`（逻辑爆破/虚晃一枪）：全场禁锢 — 未在回合切换检查
  - `oppSlowTurns`（节奏掌控）：减速 — 仅有递减，未限制移动（已知限制）
- 修复：
  - `oppMissNext`：doMove 中对方攻击时检查，打偏并跳过回合
  - `barrageActive`：doMove 吃子后检查，设置 `extraMove` 获得额外回合
  - `oppPassiveDisabled`：`triggerPassive` 开头检查，B王被动失效时跳过
  - `aoeLockdownTurns`：对方回合开始时检查，被禁锢则跳过回合
  - 所有标记在回合结束时递减
- 文件：`game.js`、`js/skills.js`

### 模块5·棋子生命周期：histEntry 浅克隆 buffs 导致悔棋 buff 错误（严重）
- 问题：`doMove` 中 `histEntry.piece = {...piece}` 浅克隆，buffs 数组共享引用。走棋时 `consumeBuff`（移除 executeMark）和 shield 数值减少会污染 `histEntry`，悔棋后 buff 状态错误。
- 修复：`histEntry` 创建时深克隆 `piece.buffs` 和 `captured.buffs`（`buffs.map(b=>({...b}))`）。
- 文件：`game.js`

### 模块5·棋子生命周期：startNewGame 未重置技能标记（严重）
- 问题：`startNewGame` 未重置 `oppMissNext`/`aoeLockdownTurns`/`oppSlowTurns`/`oppPassiveDisabled`/`barrageActive`/`bkingCdIncrease`/`skillCdReduce` 等字段，跨局残留导致下一局技能效果异常。
- 修复：`startNewGame` 中显式重置所有一次性技能标记。
- 文件：`game.js`

### 模块5·棋子生命周期：undoLastMove 未清理一次性标记（中等）
- 问题：悔棋后 `oppMissNext`/`barrageActive` 等标记未清理，可能残留影响后续逻辑。
- 修复：`undoLastMove` 中清理这两个一次性标记。
- 文件：`game.js`

### 模块7·渲染同步：passivesOnTurnStart 后未刷新 HUD（中等）
- 问题：`doMove` 回调中 `passivesOnTurnStart()` 后未调用 `renderHUD`/`updateSkillDisplay`，导致 aura/周期性被动修改状态后 HUD 不更新。
- 修复：被动触发后追加 `renderHUD()` 和 `updateSkillDisplay()`。
- 文件：`game.js`

### 已知限制（未修复）
- `oppSlowTurns`（减速）：仅有递减逻辑，未在 `getPieceMoves` 中限制移动距离（需引擎层改造，风险大）
- `makeMv` 不调用 `calcDamage`：AI minimax 评估时忽略 buff 和兵种相克（性能与准确性权衡）
- `tickBuffs` 的 `_fresh` 机制：duration=1 的 buff 多活 1 回合（设计如此，修改风险大）

---

## v16-bug-fixes-deep · 深度业务逻辑重构

### 模块5·同归于尽逻辑（致命）
- 问题：`doMove` 中双方均 hp<=0 时，只处理防守方阵亡（入阵亡名单），攻击方 hp<=0 仍占据目标位置但未入阵亡名单，导致"同归于尽"时攻击方不死。
- 修复：`doMove` 防守方阵亡分支末尾检查 `attacker.hp<=0`，若同归于尽则攻击方也入阵亡名单并清空位置；`undoLastMove` 中恢复第二个阵亡棋子（`actualAttackerCaptured`）。
- 文件：`game.js`

### 模块6·AI评估：makeMv 不调用 calcDamage（致命）
- 问题：`makeMv`（minimax 模拟用）直接用 `attacker.atk - defender.def` 计算伤害，忽略角色属性加成、buff 系统、兵种相克，导致 AI 评估与真实战斗严重不一致（如炮打近战应不掉血、车马应破甲）。
- 修复：`makeMv` 改为调用 `calcDamage` 统一计算，AI 评估与 `doMove` 真实战斗完全一致（含 buff/兵种相克/反伤/免疫）。
- 文件：`js/engine.js`

### 模块3·技能系统：oppSlowTurns 减速未实现（严重）
- 问题：`oppSlowTurns`（节奏掌控/妙语嘲讽）只递减回合数，未限制移动距离，技能完全失效。
- 修复：`aiMove` 中当 `oppSlowTurns>0` 时，过滤掉车/炮直线移动距离>1 的走法（保留马、相、士等固定步法棋子）。
- 文件：`game.js`

### 模块5·棋子生命周期：mergeBuffs duration 丢失（严重）
- 问题：`mergeBuffs` 合并 buff 时只比较 value，duration 取被保留的那个（较强值的），可能丢失持续时间更长的 buff。
- 修复：合并后 value 取较强、duration 取较长、`_fresh` 取 false（已递减过）。
- 文件：`js/engine.js`

### 模块4·被动技能：8 个 AURA 光环被动空实现（致命）
- 问题：以下光环被动 case 只有注释无实际效果，技能完全失效：
  - `p_teach`（王昕·仕相防御+15%）
  - `p_brother`（三金·棋子<8时攻击+30%）
  - `p_fullmark`（唐昊博涵·炮马攻击+15%）
  - `p_pressure`（仙帝·B王CD+1、概率-15%）
  - `p_stable`（刘佳伟·将防御+20%）
  - `p_break`（罗伦杰·破防）
  - `p_nemesis`（刘雪沛·对B王伤害+50%）
  - `p_aura`（B王·对手攻-10%）
  - `p_kingaura`（三英B王·CD-1、概率+10%）
- 修复：新增 `addBuffToPlayerPieces` 辅助函数（按角色/兵种类型加 buff）；所有光环 case 在 `aura` event 时实际加 buff 或设置标记；`calcDamage` 新增 `pierce`（破防，无视 ironwall/defenseBoost）和 `bkiller`（对B王棋子伤害加成）buff 支持；`maybeAISkill` 应用 `bkingSkillChanceReduce` 概率修正。
- 文件：`js/skills.js`、`js/engine.js`、`game.js`

### 模块6·AI评估：evaluateBoard 不考虑 buff 价值（中等）
- 问题：`evaluateBoard`（minimax 估值）只按棋子类型和 HP 折算价值，忽略 buff 影响，导致 AI 不优先攻击带虚弱 debuff 的棋子，也不保护自己带增益 buff 的棋子。
- 修复：估值时遍历 buff，有利 buff（attackBoost/ironwall/shield/immune 等）增加价值（上限+40），虚弱 buff 降低价值（-15）。
- 文件：`js/engine.js`

---

## v17-passive-display-pvp-targeting · 被动显示/PVP技能目标/深度修复

### 需求1：HUD 双方被动技能显示
- 新增 `getPassiveForColor(color)` 函数：按游戏模式获取红/黑方被动技能（PVP读 `pvpRedPassive`/`pvpBlackPassive`，PVE玩家读 `playerPassiveSkill`，AI读 `getBkingPassives()`）。
- 新增 `getPassiveTriggerLabel(trigger)` 函数：将触发类型（`on_capture`→「吃子」、`aura`→「光环」等）转为中文标签。
- HUD 新增「被动」区域（`#hud-passive-status`），显示双方被动名称+触发类型（如「连击 [吃子]」），区分主动/被动同名。
- 文件：`game.js`、`index.html`

### 需求2：故事模式定死难度
- 问题：故事模式选将后仍进入难度选择屏（`screen-handicap`），与"按章节定死难度"的设计冲突。
- 修复：`confirmCharacterSelect` 中判断 `state.storyChapterId`，故事模式直接调用 `startNewGame()`，跳过难度选择屏。难度由 `startStoryChapter` 中 `state.difficulty = ch.difficulty` 设定。
- 文件：`game.js`

### 需求3：PVP 技能目标改为对方玩家
- 问题：原 `blockOppSkill` 仅设 `aiSkillBlocked=true`（PVE 锁 B王），PVP 下无效。
- 修复：
  - 新增 `oppSkillBlockedColor` 状态（PVP 被封锁方颜色）。
  - `blockOppSkill()` 函数：PVP 设 `oppSkillBlockedColor=oppColor()`，PVE 保持 `aiSkillBlocked=true`。
  - `canUseSkill` PVP 分支：检查 `oppSkillBlockedColor===currentPlayer && silenceTurns>0` 返回 false。
  - `doMove` 回合结束：被封锁方走完时递减 `silenceTurns`，归零时清除 `oppSkillBlockedColor`。
  - `updateSkillDisplay`/`renderHUD`：PVP 被封锁方显示「沉默 N回」。
  - `undoLastMove`：快照保存/恢复 `aiSkillBlocked` 和 `oppSkillBlockedColor`。
  - `startNewGame`：重置 `oppSkillBlockedColor=null`。
- 文件：`game.js`

### 需求4：避免主动/被动技能同名
- 修复 6 处同名冲突（`data.js`）：
  - 鸡哥：被动「分身幻象」→「影分身」
  - ikun：被动「节奏掌控」→「律动本能」；被动「全给你」→「反震本能」
  - 胡浩：被动「正道护体」→「金刚护体」；被动「万法归一」→「万法归宗」
  - 刘佳伟：被动「稳如泰山」→「不动如山」
- 修复英文技能名：
  - ikun：主动「唱跳rap」→「唱跳篮球」
  - 陆星辰：主动「Debug扫描」→「代码扫描」；被动「Debug」→「除错本能」
- 文件：`js/data.js`

### 深度修复：只写不读的技能标记
1. **`oppAtkDebuff`/`oppDefDebuff` 只写不读（致命）**
   - 问题：多个技能设置 `state.oppAtkDebuff`/`state.oppDefDebuff`，但 `calcDamage` 从不读取，减攻减防效果完全无效。
   - 修复：转换为 buff 系统 — `oppAtkDebuff` → `addTeamBuff(board, opp, 'weakness', value, 1)`；新增 `defReduce` buff 类型（乘算防御削弱），`oppDefDebuff` → `addTeamBuff(board, opp, 'defReduce', value, duration)`。
   - 涉及技能：暗度陈仓/妙语嘲讽/克星之刃/豪迈冲撞/因果轮回/装逼领域
   - `calcDamage`/`getPieceEffectiveStats`/`evaluateBoard` 新增 `defReduce` 读取。
   - 文件：`game.js`、`js/engine.js`

2. **`oppCannotCapture` 只写不读（致命）**
   - 问题：刘佳伟·以退为进 设置 `state.oppCannotCapture=true`，但 `doMove` 从不检查，对方仍可吃子。
   - 修复：`doMove` 新增检查 — `oppCannotCapture && currentPlayer===skillOpp && captured` 时阻止吃子并提示。被禁吃方走完后清除标记。
   - 文件：`game.js`

3. **`aoeLockdownTurns` PVP 下禁锢方向错误（严重）**
   - 问题：`doMove` 中禁锢检查 `currentPlayer!==state.playerColor`，PVP 下黑方施法时红方不会被禁锢（`playerColor` 固定为 RED）。
   - 修复：改用 `skillOpp`（基于 `skillOwnerColor`）判断被禁锢方。
   - 文件：`game.js`

4. **`barrageActive` PVP 下连斩失效（严重）**
   - 问题：三金·兄弟连斩检查 `currentPlayer===state.playerColor`，PVP 下黑方施法时吃子后不获得额外回合。
   - 修复：改用 `skillOwner` 判断。
   - 文件：`game.js`

5. **`oppSlowTurns` PVP 下人类玩家不受减速（中等）**
   - 问题：减速仅过滤 AI 走法（`aiMove` 中），PVP 下人类对方仍可长距离移动车炮。
   - 修复：`selectPiece` 中 PVP 人类对方选车炮时，过滤 `validMoves` 中距离>1 的走法。
   - 文件：`game.js`

### 验证
- `node -c` 校验全部 JS 文件通过（ALL_OK）
- PVP 沉默持续 N 回合（与 `silenceTurns` 一致），被封锁方 HUD 显示「沉默 N回」
- 故事模式选将后直接进入游戏，跳过难度选择
- 减攻减防技能通过 buff 系统生效，HUD 兵力面板实时反映

---

## v18-side-panels-ban-grid-three-auto · 双方面板/Ban网格/三英自动轮换/网页联机

### 修复内容

1. **双方被动技能详细信息显示（各自面板）**
   - 问题：用户反馈"双方被动技能都要显示详细信息，双方玩家的面板都在各自的地方"
   - 根因：原有 HUD 仅在底部被动栏显示精简名称+触发类型，无完整描述
   - 修复：在左右侧边栏各添加 `side-panel`（左侧 opp-side-panel=黑方，右侧 my-side-panel=红方），分别显示被动技能详情（名称+触发标签+完整描述）与状态列表（棋子名+buff名+效果+剩余回合）
   - 面板按位置固定（左=黑方/B王，右=红方/玩家），与 player-card 头像布局一致
   - 新增 CSS：`.side-panel` / `.side-passive-item` / `.side-buff-item` 样式
   - 文件：`index.html`（HTML 结构）、`style.css`（样式）、`game.js`（renderHUD 填充逻辑）

2. **PVP Ban 位排版优化（网格布局）**
   - 问题：用户反馈"pvp的 ban选项排版不合理，一致垂直下来太抽象了"
   - 根因：`#ban-grid` 使用 `char-grid` class 但无对应 CSS，卡片以默认块级元素垂直排列
   - 修复：为 `#ban-grid` 添加 `display:grid; grid-template-columns: repeat(4,1fr)` 网格布局
   - 补充 `.char-card` 基础样式（背景/圆角/边框/hover 效果），移除 banned 卡片的 inline style 冲突
   - 响应式：760px 以下 3 列，480px 以下 2 列
   - 文件：`style.css`、`game.js`（移除 inline style）

3. **点击对方棋子显示动态 buff 和状态**
   - 问题：用户反馈"现在点击对方的棋子还是没有动态的buff以及状态显示"
   - 根因：canvas click 事件中，当己方已有选中棋子时点击对方棋子，仅执行 `state.selected=null` 清空选中，未进入 inspect 查看模式
   - 修复：在 `if(state.selected)` 分支中新增对方棋子检测，点击对方棋子时设置 `state.inspect={row,col}`，renderHUD 即显示该棋子属性与 buff
   - 文件：`game.js`（canvas click handler）

4. **三英模式自动轮换 + buff 随轮换改变**
   - 问题：用户反馈"三英模式不能手动轮换，而是自动轮换，轮换时期的buff也会随之改变"
   - 修复：`doMove` 回合结束时（玩家走完且非额外回合）自动调用 `switchThreeHeroAuto(nextIdx)` 切换到下一位武将
   - 武将卡片改为仅展示（移除 click 切换事件），切换按钮显示"自动轮换 · 下一位：XX"并禁用
   - `switchThreeHeroAuto` 切换时同步更新 `state.character` / `playerActiveSkill` / `playerPassiveSkill`，renderHUD 即显示新武将的被动与状态
   - 文件：`game.js`（doMove 自动轮换逻辑、renderThreeHeroesPanel 展示化）

5. **联机对战支持网页版（WebRTC）**
   - 问题：用户反馈"联机对战希望可以网页也可以联机对战，不仅仅是封装成"
   - 修复：新增 `startWebRtcHost` / `startWebRtcJoin` / `rtcWrapDC` 函数，通过 WebRTC DataChannel 实现 P2P 通信
   - 主机端生成 base64 邀请码（offer SDP），客户端粘贴邀请码生成回应码（answer SDP），双方交换后建立连接
   - DataChannel 包装为类 WebSocket 接口（readyState/send/onmessage），复用现有 `handleNetMessage` 逻辑
   - 修复 join 检测 bug：原 `ip.startsWith('{')` 无法识别 base64 邀请码，改为 IP 正则检测，非 IP 格式即走 WebRTC
   - 文件：`game.js`（WebRTC 函数、join 检测修复）、`index.html`（placeholder 提示）

### 验证
- `node -c` 校验全部 JS 文件通过
- 浏览器实测：
  - Ban 位网格：18 张卡片以 4 列网格排列，banned 卡片灰显禁用
  - 三英模式：左侧面板显示"装逼光环[光环]"（B王被动），右侧显示"谋定后动[光环]"（玩家被动），切换按钮显示"自动轮换 · 下一位：王昕"且 disabled
  - 状态栏：无 buff 时显示"无"，有 buff 时逐条列出

---

## v18-mode-lock · 阵营对战封闭 / 三英模式解锁

### 修改内容
1. **阵营对战暂时封闭**（用户反馈"阵营对战也暂时落入即将开放"）
   - HTML：`formation` 卡片添加 `disabled` class 与 `<div class="mode-locked">即 将 开 放</div>` 覆盖层
   - JS：移除 `if(selectedMode==='formation'){ showFormationMenu(); return; }` 分支，点击不再响应
   - 与 4v4 模式处理方式一致

2. **三英模式需通关故事模式**（用户反馈"三英模式也要等故事模式结束后开放"）
   - JS：`updateModeLockState()` 扩展为同时刷新 `three` 卡片的 `locked` class
   - JS：mode click handler 新增 `if(selectedMode==='three' && !isStoryCompleted()) return;` 拦截
   - 未通关时卡片显示"完成故事模式后解锁"覆盖层，与 PVE 卡片锁定逻辑一致

### 验证
- `node -c` 校验通过
- 浏览器实测：formation 卡片显示"即 将 开 放"且不可点击；three 卡片显示"完成故事模式后解锁"覆盖层
- 文件：`index.html`、`game.js`

---

## v18-three-bking-filter · 三英模式禁选B王 / 血条重叠修复

### 修复内容

1. **三英模式禁选 B王**（用户反馈"三英模式也不能有b王加入，否则还是逻辑悖论"）
   - 根因：`getCharList()` 仅在 `pve` 模式过滤 bking，三英模式未过滤，玩家可选用 B王导致"3英雄 vs B王却选了B王"的逻辑悖论
   - 修复：过滤条件扩展为 `id==='bking'&&(state.gameMode==='pve'||state.gameMode==='three')`
   - 文件：`game.js`（getCharList）

2. **血条重叠修复**（用户反馈"血条两个棋子叠加会覆盖，有没有好的解决方案"）
   - 根因：HP 血条绘制在棋子圆外下方（`barY=py+r+5`），向下延伸约 16px（条+数值文字），而相邻棋子（CELL=60, PIECE_RADIUS=25）圆顶仅距 35px，导致血条与下方棋子圆/血条互相覆盖
   - 修复方案：**血条移入棋子圆内底部**，使用 `ctx.clip()` 圆形裁剪保证不超出棋子边缘
     - 血条：宽 `r*1.5`，高 4px，y=`py+r*0.45`（圆内下沿）
     - HP 数值：7px 小号字，紧贴血条下方（仍在圆内）
     - 虚弱「虛」标记：移至棋子上方圆外（`py-r-4`），不与下方棋子冲突
   - 效果：血条完全收进棋子 50px 直径范围内，相邻棋子（间距 60px）永不重叠
   - 文件：`game.js`（drawPiece）

### 验证
- `node -c` 校验通过
- 浏览器实测：三英模式选将列表 6 人无 B王；棋子血条画在圆内，相邻棋子不再互相覆盖


---

## v19-codex-bugfix · 图鉴替换帮助 / 全局bug修复

### 修改内容

1. **图鉴替换帮助模块**
   - 欢迎屏"启动说明"按钮改为"游戏图鉴"
   - 图鉴顶部新增4个指南卡片：游戏规则/游戏流程/B王技能详解/击败B王攻略
   - 清晰区分AI专属（B王）与玩家可选角色（标签标识）
   - 移除旧help-overlay HTML
   - 文件：index.html、game.js（renderCodexGrid/renderGuidePage）、style.css

2. **P0 Bug 1修复：8个只写不读的state标记改为buff系统**
   - `state.executeMark`（刘雪沛·洞察标记）→ `addBuff(piece, 'executeMark', 0.5, 1)`
   - `state.pierceMark`（罗伦杰·破甲突袭）→ `addBuff executeMark + defReduce`
   - `state.teamShield`（王昕·考试突击）→ `addTeamBuff shield`
   - `state.immuneFirstTurn`（袁清山·潜龙勿用被动）→ `addBuff immune`
   - `state.oppAtkDecayPerTurn`（刘佳伟·后发制人）→ `addTeamBuff weakness`
   - `state.stormActive`（罗伦杰·无尽连斩）→ doMove中添加连斩读取逻辑
   - `state.hiddenPiece`（袁清山·潜龙勿用）→ drawPiece中添加雾化隐藏渲染+回合递减
   - `state.teleportUntrackable`（袁清山·隐遁闪烁）→ 保留（AI追踪逻辑复杂，暂不实现）

3. **P0 Bug 2修复：silence技能ID冲突**
   - 解宇轩·逻辑沉默 ID 从 `silence` 改为 `logic_silence`
   - 新增 `case 'logic_silence'`：沉默2回合 + 对方下回合禁吃
   - 文件：js/data.js、game.js

4. **P0 Bug 3修复：flip/saint回溯重建吃子列表用错字段**
   - `h.captured`（目标位置棋子，可能存活）改为 `h.actualCaptured`（真正被击杀的棋子）
   - 修复回溯后虚增阵亡棋子的bug
   - 文件：game.js（case 'flip'、case 'saint'）

5. **P0 Bug 6修复：三英模式CD硬编码为3**
   - canUseSkill：读取选中技能实际CD（不再硬编码3）
   - doMove CD递增：按选中技能CD作为上限
   - updateSkillDisplay：按选中技能CD显示剩余回合
   - 文件：game.js

6. **P1 Bug 7修复：oppSlowTurns在PVP中方向错误**
   - 用 skillOwnerColor/skillOpp 判断"被减速方"（原用 aiColor，PVP黑方被减速时不生效）
   - 文件：game.js（selectPiece）

### 验证
- `node -c` 校验全部 JS 文件通过（ALL_OK）
- 8个失效技能现已通过buff系统正确生效
- 三英模式技能CD按实际技能CD计算（CD=4/5的技能不再提前2回合可用）
- PVP双方减速方向修正
